export const DATA_MAPPING = {
    STATIONS: {
        endpoint: "https://demo-api.ilotusland.asia/data-insight/report/data-original/khu_cong_nghiep_protrade_3_1",
        method: "POST"
    },
    HISTORICAL: {
        api: ""
    }
}


const fetchData = (dataKey, filters) => {
    /**
     * ENDPOINT
     * - full endpoint
     * - template endpoint
     */

    /**
     * BODY
     * - build body for each endpoint
     * -> Function for each endpoint
     */


    // return standard format
}